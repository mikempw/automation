# ═══════════════════════════════════════════════════════════
# ServiceNow Mock — Environment Configuration
# ═══════════════════════════════════════════════════════════
# Place this file at: snow/.env
#   cp snow/.env.sample snow/.env
#
# The SNOW mock uses Claude to generate realistic ticket
# data and needs its own API key. It also needs the webhook
# URL from your F5 Insight instance to send tickets.
# ═══════════════════════════════════════════════════════════

# ─── LLM for ticket generation ───────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ─── F5 Insight Webhook URL ──────────────────────────────
# After creating an incoming webhook integration in the
# Insight UI (Integrations tab), copy the webhook token
# and paste it here.
#
# Format: http://<insight-host>:8000/api/integrations/webhook/<YOUR-TOKEN>
#
# If running both on the same machine via Docker:
INSIGHT_WEBHOOK_URL=http://host.docker.internal:8000/api/integrations/webhook/YOUR-WEBHOOK-TOKEN-HERE
